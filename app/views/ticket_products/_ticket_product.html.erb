<div id="<%= dom_id(ticket_product) %>" class="grid grid-cols-4 gap-4 items-center py-3">
  <div class="col-span-2">
    <p class="font-medium text-gray-900"><%= ticket_product.product.description %></p>
    <p class="text-xs text-gray-500">SKU: <%= ticket_product.product.sku %></p>
  </div>
  <div>
    <p class="text-sm text-gray-700">Cantidad: <%= ticket_product.quantity %></p>
  </div>
  <div class="text-right">
    <%# Aquí puedes añadir un botón para eliminar si lo necesitas %>
  </div>
</div>