<%= form_with(model: [ticket, ticket_comment], id: dom_id(ticket, "new_comment")) do |form| %>
  <div>
    <%= form.label :description, "Escribe tu comentario", class: "sr-only" %>
    <%= form.text_area :description, rows: 4, class: "block w-full rounded-md border-0 py-1.5 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-indigo-600 sm:text-sm sm:leading-6", placeholder: "Añade tu comentario..." %>
  </div>
  <div class="mt-3 flex justify-end">
    <%= form.submit "Publicar Comentario", class: "rounded-md bg-indigo-600 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 cursor-pointer" %>
  </div>
<% end %>