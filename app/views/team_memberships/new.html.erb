<% content_for :title, "New team membership" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New team membership</h1>

  <%= render "form", team_membership: @team_membership %>

  <%= link_to "Back to team memberships", team_memberships_path, class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
</div>
